from setuptools import setup, find_packages

setup(
    name="first_package",
    version="0.1.0",
    description="A sample Python package",
    author="Your Name",
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.6",
)